import { icons } from '@/common/utils/icons';

const DashboardIcon = icons.DashboardIcon;

const dashboard = {
  id: 'dashboard',
  title: 'dashboard',
  icon: DashboardIcon,
  type: 'group',
  url: '/dashboard'
};

export default dashboard;
