import { LAYOUT_CONST } from './constants';

const config = {
  opened: true,
  basename: '',
  defaultPath: '/',
  fontFamily: `'Poppins', san-serif`,
  borderRadius: 12,
  outlinedFilled: true,
  theme: 'light',
  presetColor: 'shobill_app',
  locale: 'en',
  layout: LAYOUT_CONST.VERTICAL_LAYOUT,
  drawerType: LAYOUT_CONST.DEFAULT_DRAWER
};

export default config;
